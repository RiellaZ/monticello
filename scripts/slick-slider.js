$('.slick__container').slick();

//I have tried every solution I can think of but it just won't line up.

$('.news__slickslider').slick({
    infinite: true,
    centerMode: true, 
    slidesToShow: 3,
    slidesToScroll: 1,
  });

  //Same here, I don't understand why it doesn't work.